<% if line_item == @current_item %>
<tr id="current_item">
<% else %>
    <tr>
        <% end %>
        <td><%= line_item.product.title %></td>
        <td><%= form_for line_item do |f| %>
            <%= f.text_field :quantity, size: 5 %>
              <!-- <%# content_for(:update_quantities) do %> -->
              <%= f.submit 'Update', method: :patch %>
              <!-- <%# end %> -->
            <% end %>
        </td>
        <!-- <td><%#= line_item.quantity %></td> -->

        <td><%= number_to_currency(line_item.product_price) %></td>
        <td class='item_price'><%= number_to_currency(line_item.total_price) %></td>

        <!-- <td>
          <%#= yield(:update_quantities) %>
        </td> -->

        <!-- <td><%#= link_to 'Delete', line_item, method: :delete,
            data: {confirm: 'Are you sure?'} %></td> -->
    </tr>
